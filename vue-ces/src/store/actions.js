// import * as api from '../axios/api'
// import responseHandle from '../axios/responseHandle'
// import router from '../router'

const actions = {
  // loginChangeUserName ({ commit, state }, name) {
  //   state.userName = name
  // },
  // loginOut ({ commit, state }) {
  //   api.userLoginOut().then(res => {
  //     responseHandle.noMessageOperate({
  //       res,
  //       sCallBack: () => {
  //         router.push({path: '/login'})
  //       }
  //     })
  //   })
  // }
}

export default actions
